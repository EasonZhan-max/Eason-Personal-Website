services:
  - type: web
    name: eason-blog
    runtime: static
    buildCommand: echo "Static site ready"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
